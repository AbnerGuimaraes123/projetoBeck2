<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <div class="formulario_wrapper sombra">
                <h2 class="projeto_titulo mb-3">Arquivos</h2>
                <ul class="list-unstyled">
                    <hr >

                    <%if(JProjetos[0].id_arq != null){%>

                        <%if(!isNaN(flagUsuario) && flagUsuario == JProjetos[0].id_user){%>
                            <% for(var i=0; i<JProjetos.length; i++) { %>
                                <li class="mb-2">
                                    <a href="/downloadArq?local=<%= JProjetos[i].url %>" class="prolado">
                                        <%= JProjetos[i].nome_arq %>
                                    </a>
                                    <span class="ml-2 text-danger"> | </span>
                                    <a href="/ExcluirArq?id_arq=<%= JProjetos[i].id_arq %>&local=<%= JProjetos[i].url %>"
                                       class="text-danger">
                                        Excluir
                                    </a>
                                </li>
                                    <hr>
                            <% } %>
                        <%}else{%>
                            <% for(var i=0; i<JProjetos.length; i++) { %>
                                <li class="mb-2">
                                    <a href="/downloadArq?local=<%= JProjetos[i].url %>" class="prolado">
                                        <%= JProjetos[i].nome_arq %>
                                    </a>
                                </li>
                                    <hr>
                            <% } %>
                        <%}%>
                    <%}else{%>
                        <li>Este projeto não tem nenhum arquivo</li>
                        
                    <%}%>

                </ul>
                <%if(JProjetos[0].id_arq != null){%>
                    <div class="mt-3">
                        <a href="/downloadProjeto?local=app\public\uploads\<%= JProjetos[0].id_projeto %>&nomeP=<%=JProjetos[0].nome_projeto%>"
                           class="btn btn-primary">
                            Download
                        </a>
                    </div>
                <%}%>
   

            </div>
        </div>
    </div>
</div>

